name: molinos_app
description: "Sistema de gestión contable para PYME - Industrial de Molinos"
publish_to: 'none'

version: 1.0.7+8

environment:
  sdk: ^3.10.0

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  # UI
  cupertino_icons: ^1.0.8
  
  # Base de datos y backend
  supabase_flutter: ^2.10.3
  sqflite: ^2.4.2
  path_provider: ^2.1.5
  path: ^1.9.1
  
  # State management y navegación
  flutter_riverpod: ^3.0.3
  go_router: ^17.0.0
  equatable: ^2.0.8
  
  # Utilidades
  intl: ^0.20.2
  uuid: ^4.5.1
  
  # Gráficos
  fl_chart: ^1.0.0
  
  # Archivos
  file_picker: ^8.0.0
  
  # Cámara
  image_picker: ^1.1.2
  
  # Impresión y PDF
  pdf: ^3.11.2
  printing: ^5.13.4
  
  # Audio (grabación de voz para asistente IA)
  record: ^5.1.2
  
  # Variables de entorno
  flutter_dotenv: ^5.2.1
  
  # NFC (lectura de tags en Android/iOS) - Temporarily disabled due to Kotlin deprecation issues
  # nfc_manager: ^3.5.0
  
  # Serialización (para modelos)
  freezed_annotation: ^3.1.0
  json_annotation: ^4.9.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  flutter_launcher_icons: ^0.14.3
  
  # Generadores de código
  build_runner: ^2.4.15
  freezed: ^3.1.0
  json_serializable: ^6.9.7

dependency_overrides:
  record_linux: ^1.3.0

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # Assets
  assets:
    - .env
    - lib/photo/logo_empresa.png

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package

# ── Launcher Icons ──────────────────────────────
flutter_launcher_icons:
  android: true
  ios: true
  windows:
    generate: true
    icon_size: 256
  image_path: "lib/photo/logo_empresa.png"
  min_sdk_android: 21
